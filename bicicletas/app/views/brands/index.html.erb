  <tbody id="marcas">
    <h3>Bienbenido a nuestro catalogo de marcas. </h1>
    <div id="general_brand">
    <% @brands.each do |brand| %>

      <div id="marcas_contenedor">
        <div id="contenedor">
            <div id="image"><%= link_to image_tag(brand.logo.url(:thumb)), :controller => "bikes" , :id => brand.id%></div>
        </div>

        <div id="confirm">
              <%= link_to '¿Que Marca es?', brand %>
            <% if !current_user.nil? %>
              <%= link_to 'Edit', edit_brand_path(brand) %>
              <%= link_to 'Destroy', brand, method: :delete, data: { confirm: 'Are you sure?' } %>
            <% end %>
        </div>
      </div>
    <% end %> 
    </div>
  </tbody>



<br>
<% if !current_user.nil? %>
<%= link_to 'New Brand', new_brand_path %>
<%= will_paginate @bikes %>
<% end %>

